#include <ctype.h>
#include "main.h"
/**
*_islower -  function that checks for lowercase character.
*@c: checking for lowercase: characters.
* Return: (1) if islower otherwise zero
*/
int _islower(int c)
{
if (islower(c))
{
return (1);

}

else

return (0);
}
