#include <stdlib.h>
#include "main.h"
#include <stdio.h>

/**
 *_abs - computes the absolute value of an integer.
 *@n : holds the value of an integer.
 * Return: Always 0.
 */
int _abs(int n)

{
	if  (abs(n))
		printf("%d\n", n);

	return (0);
}
