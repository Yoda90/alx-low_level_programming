#include "main.h"
#include <ctype.h>

/**
 * _isupper -  function that checks for uppercase character.
 *@c:  c holds the character be checked
 *
 * Return: 1 if c is capital else 0
 */
int _isupper(int c)

{
	if isupper(c)
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
