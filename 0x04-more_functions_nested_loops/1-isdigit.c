#include "main.h"
#include <ctype.h>

/**
 *  _isdigit -  function that checks for a digit (0 through 9).
 *@c:  c holds the character be checked
 *
 * Return: 1 if c is checks for digit  else 0
 */
int _isdigit(int c)

{
	if isgigit(c)
	{
		return (1);
	}

	else
	{
		return (0);
	}
}

