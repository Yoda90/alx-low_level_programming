#include "main.h"
#include <string.h>
/**
 *_strlen - calculate length of a string.
 *@s : holds length of a string.
 *
 *Return: 16
 */
int _strlen(char *s)
{

	printf("length of a %s\n", (s));

	return (16);
}

