#include "main.h"
#include <string.h>
/**
 *_strlen - calculate length of a string.
 *@s : holds length of a string.
 *
 *Return: 16
 */
int _strlen(char *s)
{

	int length = strlen(s);

	return (length);
}

