#include "main.h"
#include <string.h>
/**
 *_strcpy - function that copies the string pointed to by src
 *@dest : hold function that copy a string.
 *@src : hold function that copy a string.
 *Return:dest
 */

char *_strcpy(char *dest, char *src)

{

	strcpy(dest, src);
	return (dest);

}
